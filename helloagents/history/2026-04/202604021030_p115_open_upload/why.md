# 背景
当前项目的普通上传依赖 `p115client.upload_file`，而 plugin 使用 115 Open 上传链路（init/get_token/resume + OSS 分片），两者在可控性、重试和中断点上存在差异。

# 目标
将当前服务的上传链路尽量对齐 plugin 的 Open 方式，同时保留现有任务调度、目录缓存、实时日志与中断能力。

# 范围
- 后端 115 网关与上传策略
- 运行日志与取消检查点兼容
- 配置与文档同步
